{
  "$schema": "https://biomejs.dev/schemas/2.5.10/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "includes": ["**", "!**/dist", "!**/.gitkeep", "!docs", "!**/src/generated"]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "double"
    }
  },
  "linter": {
    "enabled": true,
    "rules": {
      "preset": "recommended",
      "style": {
        "noNamespace": "error",
        "noEnum": "error",
        "noRestrictedImports": {
          "level": "error",
          "options": {
            "paths": {
              "vscode": "Only packages/host-vscode may import 'vscode' (B1, §1.2 C1).",
              "electron": "No package imports 'electron': v1 ships no Electron host. A future host package would get its own override, as host-vscode has for 'vscode' (§1.2 C1, §2.2).",
              "bun:test": "bun:test is only for *.test.ts files and scripts/** (B2, §8.1).",
              "bun:sqlite": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
              "bun:ffi": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
              "bun:jsc": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1)."
            }
          }
        },
        "noRestrictedGlobals": {
          "level": "error",
          "options": {
            "deniedGlobals": {
              "Bun": "The Bun global is banned outside scripts/** and *.test.ts (B2, §8.1)."
            }
          }
        }
      },
      "suspicious": {
        "noExplicitAny": "warn"
      }
    }
  },
  "overrides": [
    {
      "includes": ["packages/core/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "vscode": "Only packages/host-vscode may import 'vscode' (B1, §1.2 C1).",
                  "electron": "No package imports 'electron': v1 ships no Electron host. A future host package would get its own override, as host-vscode has for 'vscode' (§1.2 C1, §2.2).",
                  "bun:test": "bun:test is only for *.test.ts files and scripts/** (B2, §8.1).",
                  "bun:sqlite": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "bun:ffi": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "bun:jsc": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "@kira-version/git": "B3 — core depends on nothing (§3.1).",
                  "@kira-version/ipc": "B3 — core depends on nothing (§3.1).",
                  "@kira-version/ui": "B3 — core depends on nothing (§3.1).",
                  "@kira-version/host-vscode": "B3 — core depends on nothing (§3.1)."
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/ipc/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "vscode": "Only packages/host-vscode may import 'vscode' (B1, §1.2 C1).",
                  "electron": "No package imports 'electron': v1 ships no Electron host. A future host package would get its own override, as host-vscode has for 'vscode' (§1.2 C1, §2.2).",
                  "bun:test": "bun:test is only for *.test.ts files and scripts/** (B2, §8.1).",
                  "bun:sqlite": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "bun:ffi": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "bun:jsc": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "@kira-version/core": "B3 — ipc depends on nothing (§3.1).",
                  "@kira-version/git": "B3 — ipc depends on nothing (§3.1).",
                  "@kira-version/ui": "B3 — ipc depends on nothing (§3.1).",
                  "@kira-version/host-vscode": "B3 — ipc depends on nothing (§3.1)."
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/git/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "vscode": "Only packages/host-vscode may import 'vscode' (B1, §1.2 C1).",
                  "electron": "No package imports 'electron': v1 ships no Electron host. A future host package would get its own override, as host-vscode has for 'vscode' (§1.2 C1, §2.2).",
                  "bun:test": "bun:test is only for *.test.ts files and scripts/** (B2, §8.1).",
                  "bun:sqlite": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "bun:ffi": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "bun:jsc": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "@kira-version/ui": "B3 — git may import core + ipc only (§3.1).",
                  "@kira-version/host-vscode": "B3 — git may import core + ipc only (§3.1)."
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/ui/**", "apps/harness/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "vscode": "Only packages/host-vscode may import 'vscode' (B1, §1.2 C1).",
                  "electron": "No package imports 'electron': v1 ships no Electron host. A future host package would get its own override, as host-vscode has for 'vscode' (§1.2 C1, §2.2).",
                  "bun:test": "bun:test is only for *.test.ts files and scripts/** (B2, §8.1).",
                  "bun:sqlite": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "bun:ffi": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "bun:jsc": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "@kira-version/git": "B3 — ui may import core + ipc only (§3.1).",
                  "@kira-version/host-vscode": "B3 — ui may import core + ipc only (§3.1)."
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/host-vscode/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "electron": "No package imports 'electron': v1 ships no Electron host. A future host package would get its own override, as host-vscode has for 'vscode' (§1.2 C1, §2.2).",
                  "bun:test": "bun:test is only for *.test.ts files and scripts/** (B2, §8.1).",
                  "bun:sqlite": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "bun:ffi": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1).",
                  "bun:jsc": "The bun: runtime is banned outside scripts/** and *.test.ts (B2, §8.1)."
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["scripts/**", "**/*.test.ts"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "vscode": "Only packages/host-vscode may import 'vscode' (B1, §1.2 C1).",
                  "electron": "No package imports 'electron': v1 ships no Electron host. A future host package would get its own override, as host-vscode has for 'vscode' (§1.2 C1, §2.2)."
                }
              }
            },
            "noRestrictedGlobals": "off"
          }
        }
      }
    },
    {
      "includes": ["**/*.vue"],
      "linter": {
        "rules": {
          "correctness": {
            "noUnusedImports": "off",
            "noUnusedVariables": "off"
          },
          "style": {
            "useImportType": "off"
          }
        }
      }
    }
  ]
}
